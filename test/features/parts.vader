Before (Set a part):
  call modestatus#parts#add('test1', 'test1')

Execute (Ensure the part was added):
  Assert modestatus#parts#has('test1')

Execute (Ensure the part has the correct associated function):
  AssertEqual 'test1', modestatus#parts#get('test1')

Execute (Ensure that the part can be overriden):
  call modestatus#parts#add('test1', 'override')
  AssertEqual 'override', modestatus#parts#get('test1')

Before (Set up multiple parts):
  call modestatus#parts#add('test1', 'test1')
  call modestatus#parts#add('test2', 'test2')
  call modestatus#parts#add('test3', 'test3')

Execute (Ensure all parts were added):
  Assert modestatus#parts#has('test1')
  Assert modestatus#parts#has('test2')
  Assert modestatus#parts#has('test3')

Execute (Ensure all parts have the correct associated function):
  AssertEqual 'test1', modestatus#parts#get('test1')
  AssertEqual 'test2', modestatus#parts#get('test2')
  AssertEqual 'test3', modestatus#parts#get('test3')

Execute (Ensure checking for an option part returns false):
  Assert !modestatus#parts#has('unknown')

Execute (Ensure getting an unknown part raises an error):
  Assert !modestatus#parts#get('unknown')
